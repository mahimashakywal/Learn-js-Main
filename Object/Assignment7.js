// Write a JavaScript program to get the length of a JavaScript object.
const student2 = {
    name : "David Rayy",
    sclass : "VI",
    rollno : 12
};

const length1 = Object.keys(student2);
console.log(length1.length);